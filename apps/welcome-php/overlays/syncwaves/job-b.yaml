apiVersion: batch/v1
kind: Job
metadata:
  name: job-b
  annotations:
    argocd.argoproj.io/sync-wave: "21"
spec:
  template:
    spec:
      containers:
      - name: busybox
        image: busybox
        args:
        - /bin/sh
        - -c
        - sleep 5
      restartPolicy: Never
  backoffLimit: 4
